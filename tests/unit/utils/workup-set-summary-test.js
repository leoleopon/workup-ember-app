import workupSetSummary from 'workup-ember-app/utils/workup-set-summary';
import { module, test } from 'qunit';

module('Unit | Utility | workup set summary');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = workupSetSummary();
  assert.ok(result);
});
