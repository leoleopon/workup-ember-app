import athleteStateSummary from 'workup-ember-app/utils/athlete-state-summary';
import { module, test } from 'qunit';

module('Unit | Utility | athlete state summary');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = athleteStateSummary();
  assert.ok(result);
});
