import workupSummary from 'workup-ember-app/utils/workup-summary';
import { module, test } from 'qunit';

module('Unit | Utility | workup summary');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = workupSummary();
  assert.ok(result);
});
